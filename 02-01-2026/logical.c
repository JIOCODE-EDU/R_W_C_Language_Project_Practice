#include <stdio.h>
#include <stdbool.h>

int main(){

  // Logical AND (&&)
  // Logical OR (||)
  // Logical NOT (!)
  

  bool a = false;
  bool b = true;
  bool c = true;

  if(!a){
    printf("Hello World!");
  }

  return 0;
}